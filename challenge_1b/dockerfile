# Use a lightweight Python base image
FROM python:3.9-slim

# Set the working directory in the container
WORKDIR /app

# Copy the requirements file and install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy the Python script and the input JSON configuration file
COPY generic_planner.py .
COPY challenge1b_input.json .

# Copy all PDF documents from the build context into the container
# NOTE: Place your PDFs in the same directory as the Dockerfile before building.
COPY *.pdf ./

# Command to run the application
CMD ["python", "generic_planner.py"]